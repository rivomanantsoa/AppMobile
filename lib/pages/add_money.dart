import 'package:flutter/material.dart';

/// Flutter code sample for [PopupRoute].
class AddMoney extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Add Money Page"));
  }
}